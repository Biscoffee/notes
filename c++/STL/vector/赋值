void test01()
{
    vector<int>v1;
    for(int i = 0;i < 10;i++){
        v1.pushback(i)
    }
    printvector(v1);

    vector<int>v2;
    v2 = v1;
    printvector(v2);

    vector<int>v3;
    v3.assign(v1.begin, v1.end);

    vector<int>v4;
    v4.assign(10,100); //将10个100赋值给本身
    printVector(v4);
}