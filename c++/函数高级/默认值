int func (int a, int b, int c){
    return a + b + c;
}
注意事项：
1. 如果某个位置有了默认参数，从这个位置以后，从左到右必须有默认值
2. 如果函数声明有默认参数，函数实现就不能有默认参数

int func(int a = 10, int b = 10, int c = 20);
int func(int a, int b, int c){
    return a + b + c;
}